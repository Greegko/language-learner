name: website

on: push

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: install dependencies
        run: yarn
      -
        name: build
        run: yarn build
      -
        name: deploy
        run: yarn deploy